{
  "name": "resume-builder-server",
  "version": "1.0.0",
  "description": "Backend для генерации резюме в PDF",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "cd ../client && npm run build && cd ../server",
    "prod": "npm run build && npm start"
  },
  "dependencies": {
    "@sparticuz/chromium": "^119.0.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "fs": "^0.0.1-security",
    "multer": "^1.4.5-lts.1",
    "path": "^0.12.7",
    "puppeteer-core": "^19.11.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
